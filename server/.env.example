DATABASE_URL="file:./dev.db"
PORT=4000

# Optional. Register at https://data.bls.gov/registrationEngine/ for a higher
# rate limit and multi-year history. The simulator works without it.
# BLS_API_KEY=""

# Optional. Set this to enable Atlas's real-time LLM tutoring. Without a key,
# Atlas falls back to a deterministic rule-based mentor that still works.
# Get a key at https://console.anthropic.com/
# ANTHROPIC_API_KEY=""
# MENTOR_MODEL="claude-haiku-4-5-20251001"
